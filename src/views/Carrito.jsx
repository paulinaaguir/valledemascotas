import React from "react";
import Producto from "../components/Producto";
import Productos from "./Productos";

const Carrito = (productosEnCarrito) => {
  const carrito = [];
  carrito.push(productosEnCarrito);
  return "";
};

export default Carrito;
